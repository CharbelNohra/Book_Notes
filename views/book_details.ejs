<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Book Details - <%= book.title %>
        </title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="../public/styles.css">
    </head>

    <body>
        <div class="container mt-5">
            <!-- Book Details Section -->
            <div class="row">
                <div class="col-lg-6 mb-4">
                    <h1 class="text-primary">
                        <%= book.title %>
                    </h1>
                    <p class="text-muted">by <%= book.author %>
                    </p>

                    <% if (book.cover_image_url) { %>
                        <img src="<%= book.cover_image_url %>" class="img-fluid rounded mb-3" alt="Book Cover"
                            style="max-width: 300px;">
                        <% } %>

                            <p class="mt-3"><strong>Rating:</strong>
                                <%= book.rating %> / 5.0
                            </p>
                            <p><strong>Notes:</strong></p>
                            <p>
                                <%= book.notes %>
                            </p>

                            <a href="/" class="btn btn-outline-primary mt-4">Back to Book List</a>
                </div>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>

</html>